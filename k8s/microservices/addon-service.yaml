apiVersion: v1
kind: Service
metadata:
  name: addon-service
  namespace: starlight-mesh
  labels:
    app: addon-service
spec:
  ports:
    - port: 8089
      targetPort: 8089
      name: http
  selector:
    app: addon-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: addon-service
  namespace: starlight-mesh
spec:
  replicas: 1
  selector:
    matchLabels:
      app: addon-service
  template:
    metadata:
      labels:
        app: addon-service
    spec:
      containers:
        - name: addon-service
          image: starlight-stays/addon-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8089
              name: http
          env:
            - name: SPRING_PROFILES_ACTIVE
              valueFrom:
                configMapKeyRef:
                  name: starlight-config
                  key: SPRING_PROFILES_ACTIVE
            - name: EUREKA_CLIENT_SERVICEURL_DEFAULTZONE
              valueFrom:
                configMapKeyRef:
                  name: starlight-config
                  key: EUREKA_SERVER_URL
            - name: SPRING_DATASOURCE_URL
              value: jdbc:postgresql://postgres.starlight-mesh.svc.cluster.local:5432/starlight_addons
            - name: SPRING_DATASOURCE_USERNAME
              valueFrom:
                secretKeyRef:
                  name: starlight-secrets
                  key: POSTGRES_USER
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: starlight-secrets
                  key: POSTGRES_PASSWORD
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8089
            initialDelaySeconds: 20
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8089
            initialDelaySeconds: 40
            periodSeconds: 15
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
