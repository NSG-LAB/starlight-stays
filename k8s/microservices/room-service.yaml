apiVersion: v1
kind: Service
metadata:
  name: room-service
  namespace: starlight-mesh
  labels:
    app: room-service
spec:
  ports:
    - port: 8082
      targetPort: 8082
      name: http
  selector:
    app: room-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: room-service
  namespace: starlight-mesh
spec:
  replicas: 2
  selector:
    matchLabels:
      app: room-service
  template:
    metadata:
      labels:
        app: room-service
    spec:
      containers:
        - name: room-service
          image: starlight-stays/room-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8082
              name: http
          env:
            - name: SPRING_PROFILES_ACTIVE
              valueFrom:
                configMapKeyRef:
                  name: starlight-config
                  key: SPRING_PROFILES_ACTIVE
            - name: EUREKA_CLIENT_SERVICEURL_DEFAULTZONE
              valueFrom:
                configMapKeyRef:
                  name: starlight-config
                  key: EUREKA_SERVER_URL
            - name: SPRING_DATASOURCE_URL
              value: jdbc:postgresql://postgres.starlight-mesh.svc.cluster.local:5432/starlight_rooms
            - name: SPRING_DATASOURCE_USERNAME
              valueFrom:
                secretKeyRef:
                  name: starlight-secrets
                  key: POSTGRES_USER
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: starlight-secrets
                  key: POSTGRES_PASSWORD
            - name: SPRING_DATA_REDIS_HOST
              valueFrom:
                configMapKeyRef:
                  name: starlight-config
                  key: REDIS_HOST
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8082
            initialDelaySeconds: 20
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8082
            initialDelaySeconds: 40
            periodSeconds: 15
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
